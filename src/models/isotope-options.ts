/**
 * IsotopeOptions
 *  Este archivo no es ac"cedido por ningún componente, pero se dejá aqui para poder consultar las
 *  OPTIONS de la librería ISOTOPE, al momento de armar el "".grid"
 */
export interface IsotopeOptions {
  itemSelector?: string;
  columnWidth?: number | string;
  gutter?: number | string;
  percentPosition?: boolean;
  stamp?: string;
  fitWidth?: boolean;
  originLeft?: boolean;
  originTop?: boolean;
  containerStyle?: string;
  transitionDuration?: string;
  resize?: boolean;
  initLayout?: boolean;
  stagger?: number | string;
}
